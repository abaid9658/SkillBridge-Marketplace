apiVersion: v1
kind: Service
metadata:
  name: skillbridge-client-svc
  namespace: skillbridge
  labels:
    app: skillbridge-client
spec:
  type: ClusterIP
  selector:
    app: skillbridge-client
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
