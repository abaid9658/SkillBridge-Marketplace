apiVersion: v1
kind: ConfigMap
metadata:
  name: skillbridge-config
  namespace: skillbridge
data:
  NODE_ENV: "production"
  PORT: "5000"
  JWT_EXPIRE: "7d"
  EMAIL_HOST: "smtp.gmail.com"
  EMAIL_PORT: "587"
  # CLIENT_URL is set per environment in the deployment
